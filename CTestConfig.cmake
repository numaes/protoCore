# CTest configuration for protoCore.
# Optional: set CTEST_PARALLEL_LEVEL for default parallel test runs (e.g. in CI).
# Example: set(CTEST_PARALLEL_LEVEL 4)
# If unset, use "ctest -j N" (e.g. ctest -j$(nproc)) when running tests.
